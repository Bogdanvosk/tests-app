import { getUserAction, signInAction, signUpAction, logoutAction } from '.';

export const SIGN_IN = signInAction.type;
export const SIGN_UP = signUpAction.type;
export const GET_USER = getUserAction.type;
export const LOGOUT = logoutAction.type;
